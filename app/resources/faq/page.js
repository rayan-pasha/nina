import PlaceholderPage, { placeholderMetadata } from "@/components/PlaceholderPage";

export const metadata = placeholderMetadata("FAQ", "/resources/faq");

export default function Page() {
  return <PlaceholderPage title="FAQ" />;
}

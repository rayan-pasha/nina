import PlaceholderPage, { placeholderMetadata } from "@/components/PlaceholderPage";

export const metadata = placeholderMetadata("Security", "/resources/security");

export default function Page() {
  return <PlaceholderPage title="Security" />;
}
